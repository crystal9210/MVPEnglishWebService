// 複数ユーザ間で履歴を共有するサービスを追加するときこんな感じ
// import type { UserHistoryItem } from "@/schemas/userHistorySchemas";

// export interface IHistoryService {
//     listAllEvents(): Promise<UserHistoryItem[]>; // 全ユーザのイベントを取得する例（グローバルな履歴アクセス）
// }
